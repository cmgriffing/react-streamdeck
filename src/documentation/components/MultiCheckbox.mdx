---
name: MultiCheckbox
route: /components/multi-checkbox
menu: 🔌 Components
---

# MultiCheckbox

import { Props, Playground } from "docz";
import SDMultiCheckbox from "../../components/MultiCheckbox";
import DemoStateContainer from "react-component-component";
import { multiCheckboxOptions } from '../fixtures'

## Example

<Playground>
  <div class="docz-sdpi-wrapper">
    <DemoStateContainer
      initialState={{
        multiCheckboxState: multiCheckboxOptions
      }}
    >
      {({setState, state: { multiCheckboxState }}) => (
        <SDMultiCheckbox
          label="MultiCheckbox"
          options={multiCheckboxState}
          onChange={(_, value) => {
            setState({
              multiCheckboxState: value
            });
          }}
        />
      )}
    </DemoStateContainer>
  </div>
</Playground>

## Props

<Props of={SDMultiCheckbox} />
